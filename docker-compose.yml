services:
  telegram-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: usdt_monitor_bot
    env_file:
      - .env
    volumes:
      - data:/app/data
    restart: unless-stopped

volumes:
  data:
